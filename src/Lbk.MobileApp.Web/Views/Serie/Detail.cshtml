@model Lbk.MobileApp.Web.Models.SerieFormModel
@{
    ViewBag.Title = "Quiz - [Detail Serie]";
}
<h2>
    Detail</h2>
<fieldset>
    <legend>Detail Serie</legend>
    <div class="display-label">
        ActivatedAt</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.ActivatedAt)
    </div>
    <div class="display-label">
        Description</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Description)
    </div>
    <div class="display-label">
        ExpiresAt</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.ExpiresAt)
    </div>
    <div class="display-label">
        IsActivated</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.IsActivated)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "List")
</p>
